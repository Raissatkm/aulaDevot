/*Crie um objeto configuracao que contenha tema: "escuro" e idioma: "pt-BR". Congele esse objeto para evitar que seja modificado.
configuracao.tema = "claro";
 // Essa alteração deve ser ignoradaconsole.log(configuracao.tema); // "escuro" */
const configuracao = Object.freeze({
    tema: escuro,
    idioma: pt-BR,



})
configuracao.tema = "claro";
console.log(configuracao.tema); 