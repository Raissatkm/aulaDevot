/*- **Objetivo:** Use um `while` para criar loops baseados em condições.
    1. Crie um loop que imprime números de 1 a 10 usando `while`.*/

let contador = 1;while (contador < 11){
    console.log(contador);
    contador++;
}